{
  "data": {
    "id": {{ page.id|json_encode() }},
    "title": {{ page.title|json_encode() }},
    "url": {{ url(page, { canonical: true, format: 'json' })|json_encode() }}{% if page.body is defined %},
    "body": {{ page.body|json_encode() }}
{% endif %}
  }
}
