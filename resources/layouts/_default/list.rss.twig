{% extends 'extended/feed.twig' %}

{% block feed %}
<?xml version="1.0" encoding="utf-8"?>
{%- if site.page('xsl/rss') ~%}
<?xml-stylesheet type="text/xsl" href="{{ url('xsl/rss', {canonical: true}) }}" media="all"?>
{%- endif ~%}
<rss version="2.0">
  <channel>
    <title>{{ title|e }}</title>
    <description><![CDATA[{{ site.description }}]]></description>
    <lastBuildDate>{{ updated|date('c') }}</lastBuildDate>
    <link href="{{ url(page, {canonical: true, format: 'rss'}) }}" rel="self" type="application/rss+xml" />
    <link href="{{ url(page, {canonical: true, format: 'html'}) }}" rel="alternate" type="text/html" />
    {%- for item in items ~%}
    <item>
      {%- block item ~%}
      <guid>{{ url(item, {canonical: true}) }}</guid>
      <title>{{ item.title|e }}</title>
      <pubDate>{{ item.date|date('c') }}</pubDate>
      <link href="{{ url(item, {canonical: true}) }}" rel="alternate" type="text/html" />
      <description><![CDATA[{{ item.content }}]]></description>
      {%- endblock ~%}
    </item>
    {%- endfor ~%}
  </channel>
</rss>
{% endblock feed %}