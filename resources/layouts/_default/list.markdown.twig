{% set pages = site.pages.showable|filter_by('section', page.section)|sort_by_date|sort_by_weight %}

{% extends ['page.markdown.twig', '_default/page.markdown.twig'] %}

{%- block content %}
# {{ page.title|trim }}

> {{ page.body|trim }}

{% for item in pages %}
- [{{ item.title|trim }}]({{ url(item, {format: 'markdown', canonical: true}) }})
{% endfor %}
{% endblock %}