      <nav>
        <ul>
          {%- for item in site.menus.main|sort_by_weight ~%}
          <li>
            <a href="{{ url(item.url) }}">{{ item.name }}</a>
          </li>
          {%- endfor ~%}
        </ul>
      </nav>