# Cecil Skeleton

This folder contains a starter skeleton for a Cecil site.

## Structure

- `cecil.yml`: site configuration
- `assets/`: styles, scripts, and other source assets
- `layouts/`: Twig templates
- `pages/`: Markdown content pages
- `static/`: static files copied as-is to the output
