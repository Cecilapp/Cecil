<!DOCTYPE html>
<html lang="en">
{% include 'include/head.html.twig' %}
  <body>
    <div class="container content">
      <header class="masthead">
        <h3 class="masthead-title">
          <a href="{{ site.baseurl }}" title="Home">{{ site.title }}</a>
          <small>{{ site.baseline }}</small>
        </h3>
      </header>
      <main>
        {{ page.content }}
      </main>
      <footer class="footer">
        <small>
          &copy; <time datetime="{{ site.timestamp|date('Y-m-dTH:i:sP') }}">{{ site.timestamp|date('Y') }}</time>. All rights reserved.
        </small>
      </footer>
    </div>
  </body>
</html>
