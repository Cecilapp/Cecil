{% extends '_default/page.html.twig' %}

{% block content %}
{% for p in site.pages|filterBy('section', page.pathname)|sortByDate %}
        <h1><a href="{{ url(p) }}">{{ p.title|e }}</a></h1>
        {{ p.content|excerptHtml }}
{% endfor %}
{% endblock content %}
